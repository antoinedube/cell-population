version: 2
jobs:
  test:
    docker:
      - image: debian:stretch
    steps:
      - run: sudo apt-get update
      - run: sudo apt-get -y install cmake make gcc
      - checkout
      - run: cmake --version
      - run: make --version
workflows:
    version: 2
    build_and_test:
      jobs:
        - test
